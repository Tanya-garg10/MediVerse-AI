services:
  - type: web
    name: mediverse-ai
    runtime: node
    buildCommand: bun install && bun run build
    startCommand: node start.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
